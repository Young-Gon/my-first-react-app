# 스프레드 연산자 (Spread Operator: `...`)

스프레드 연산자는 ES6(ECMAScript 2015)에서 도입된 문법으로, 배열이나 객체와 같은 반복 가능한(iterable) 대상을 개별 요소로 분리하여 확장할 때 사용합니다.

---

## 1. 배열에서의 활용

배열의 요소를 복사하거나 병합할 때 매우 직관적입니다.

```javascript
const arr1 = [1, 2, 3];
const arr2 = [4, 5];

// 배열 병합
const combined = [...arr1, ...arr2]; // [1, 2, 3, 4, 5]

// 요소 추가
const newArr = [0, ...arr1, 4]; // [0, 1, 2, 3, 4]
```

---

## 2. 객체에서의 활용

기존 객체를 복사하면서 특정 값을 수정하거나 새로운 속성을 추가할 때 유용합니다.

```javascript
const user = { name: 'User', age: 25 };

// 객체 복사 및 속성 수정/추가
const updatedUser = { ...user, age: 26, location: 'Seoul' };
// 결과: { name: 'User', age: 26, location: 'Seoul' }
```

---

## 3. 리액트 컴포넌트 Props 전달

리액트에서 부모 컴포넌트의 데이터를 자식에게 한꺼번에 넘길 때 자주 사용됩니다.

### 명시적 전달 (Explicit)
```typescriptreact
<HelloWorld name="User" age={25} />
```
*   어떤 값이 전달되는지 명확히 보입니다.

### 스프레드 연산자 전달 (Spread)
```typescriptreact
const props = { name: 'User', age: 25 };
<HelloWorld {...props} />
```
*   코드가 간결해지며, `props` 객체의 모든 키-값이 속성으로 자동 전달됩니다.

---

## 4. 주의사항: 얕은 복사 (Shallow Copy)

스프레드 연산자는 **얕은 복사**를 수행합니다. 즉, 객체 내부에 또 다른 객체나 배열이 있을 경우, 그 내부의 참조값까지 완전히 새로 복사하지는 않습니다.

```javascript
const original = { a: 1, b: { c: 2 } };
const copy = { ...original };
copy.b.c = 99; // original.b.c도 99로 바뀜 (참조 공유)
```